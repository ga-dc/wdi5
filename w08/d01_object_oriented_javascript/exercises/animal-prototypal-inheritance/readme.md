# Animal Prototypal Inheritance

Create a `Cow` constructor function, and link its prototype to a new `Animal`

Define a moo method for your Cow object, which returns the string "moo", plus the Cow's name.